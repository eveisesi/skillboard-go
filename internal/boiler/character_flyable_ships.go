// Code generated by SQLBoiler 4.8.3 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package boiler

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CharacterFlyableShip is an object representing the database table.
type CharacterFlyableShip struct {
	CharacterID uint64    `boil:"character_id" json:"character_id" toml:"character_id" yaml:"character_id"`
	ShipTypeID  uint      `boil:"ship_type_id" json:"ship_type_id" toml:"ship_type_id" yaml:"ship_type_id"`
	Flyable     uint8     `boil:"flyable" json:"flyable" toml:"flyable" yaml:"flyable"`
	CreatedAt   time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *characterFlyableShipR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L characterFlyableShipL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CharacterFlyableShipColumns = struct {
	CharacterID string
	ShipTypeID  string
	Flyable     string
	CreatedAt   string
}{
	CharacterID: "character_id",
	ShipTypeID:  "ship_type_id",
	Flyable:     "flyable",
	CreatedAt:   "created_at",
}

var CharacterFlyableShipTableColumns = struct {
	CharacterID string
	ShipTypeID  string
	Flyable     string
	CreatedAt   string
}{
	CharacterID: "character_flyable_ships.character_id",
	ShipTypeID:  "character_flyable_ships.ship_type_id",
	Flyable:     "character_flyable_ships.flyable",
	CreatedAt:   "character_flyable_ships.created_at",
}

// Generated where

var CharacterFlyableShipWhere = struct {
	CharacterID whereHelperuint64
	ShipTypeID  whereHelperuint
	Flyable     whereHelperuint8
	CreatedAt   whereHelpertime_Time
}{
	CharacterID: whereHelperuint64{field: "`character_flyable_ships`.`character_id`"},
	ShipTypeID:  whereHelperuint{field: "`character_flyable_ships`.`ship_type_id`"},
	Flyable:     whereHelperuint8{field: "`character_flyable_ships`.`flyable`"},
	CreatedAt:   whereHelpertime_Time{field: "`character_flyable_ships`.`created_at`"},
}

// CharacterFlyableShipRels is where relationship names are stored.
var CharacterFlyableShipRels = struct {
	Character string
	ShipType  string
}{
	Character: "Character",
	ShipType:  "ShipType",
}

// characterFlyableShipR is where relationships are stored.
type characterFlyableShipR struct {
	Character *User `boil:"Character" json:"Character" toml:"Character" yaml:"Character"`
	ShipType  *Type `boil:"ShipType" json:"ShipType" toml:"ShipType" yaml:"ShipType"`
}

// NewStruct creates a new relationship struct
func (*characterFlyableShipR) NewStruct() *characterFlyableShipR {
	return &characterFlyableShipR{}
}

// characterFlyableShipL is where Load methods for each relationship are stored.
type characterFlyableShipL struct{}

var (
	characterFlyableShipAllColumns            = []string{"character_id", "ship_type_id", "flyable", "created_at"}
	characterFlyableShipColumnsWithoutDefault = []string{"character_id", "ship_type_id", "created_at"}
	characterFlyableShipColumnsWithDefault    = []string{"flyable"}
	characterFlyableShipPrimaryKeyColumns     = []string{"character_id", "ship_type_id"}
)

type (
	// CharacterFlyableShipSlice is an alias for a slice of pointers to CharacterFlyableShip.
	// This should almost always be used instead of []CharacterFlyableShip.
	CharacterFlyableShipSlice []*CharacterFlyableShip
	// CharacterFlyableShipHook is the signature for custom CharacterFlyableShip hook methods
	CharacterFlyableShipHook func(context.Context, boil.ContextExecutor, *CharacterFlyableShip) error

	characterFlyableShipQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	characterFlyableShipType                 = reflect.TypeOf(&CharacterFlyableShip{})
	characterFlyableShipMapping              = queries.MakeStructMapping(characterFlyableShipType)
	characterFlyableShipPrimaryKeyMapping, _ = queries.BindMapping(characterFlyableShipType, characterFlyableShipMapping, characterFlyableShipPrimaryKeyColumns)
	characterFlyableShipInsertCacheMut       sync.RWMutex
	characterFlyableShipInsertCache          = make(map[string]insertCache)
	characterFlyableShipUpdateCacheMut       sync.RWMutex
	characterFlyableShipUpdateCache          = make(map[string]updateCache)
	characterFlyableShipUpsertCacheMut       sync.RWMutex
	characterFlyableShipUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var characterFlyableShipBeforeInsertHooks []CharacterFlyableShipHook
var characterFlyableShipBeforeUpdateHooks []CharacterFlyableShipHook
var characterFlyableShipBeforeDeleteHooks []CharacterFlyableShipHook
var characterFlyableShipBeforeUpsertHooks []CharacterFlyableShipHook

var characterFlyableShipAfterInsertHooks []CharacterFlyableShipHook
var characterFlyableShipAfterSelectHooks []CharacterFlyableShipHook
var characterFlyableShipAfterUpdateHooks []CharacterFlyableShipHook
var characterFlyableShipAfterDeleteHooks []CharacterFlyableShipHook
var characterFlyableShipAfterUpsertHooks []CharacterFlyableShipHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CharacterFlyableShip) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterFlyableShipBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CharacterFlyableShip) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterFlyableShipBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CharacterFlyableShip) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterFlyableShipBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CharacterFlyableShip) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterFlyableShipBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CharacterFlyableShip) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterFlyableShipAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CharacterFlyableShip) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterFlyableShipAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CharacterFlyableShip) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterFlyableShipAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CharacterFlyableShip) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterFlyableShipAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CharacterFlyableShip) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterFlyableShipAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCharacterFlyableShipHook registers your hook function for all future operations.
func AddCharacterFlyableShipHook(hookPoint boil.HookPoint, characterFlyableShipHook CharacterFlyableShipHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		characterFlyableShipBeforeInsertHooks = append(characterFlyableShipBeforeInsertHooks, characterFlyableShipHook)
	case boil.BeforeUpdateHook:
		characterFlyableShipBeforeUpdateHooks = append(characterFlyableShipBeforeUpdateHooks, characterFlyableShipHook)
	case boil.BeforeDeleteHook:
		characterFlyableShipBeforeDeleteHooks = append(characterFlyableShipBeforeDeleteHooks, characterFlyableShipHook)
	case boil.BeforeUpsertHook:
		characterFlyableShipBeforeUpsertHooks = append(characterFlyableShipBeforeUpsertHooks, characterFlyableShipHook)
	case boil.AfterInsertHook:
		characterFlyableShipAfterInsertHooks = append(characterFlyableShipAfterInsertHooks, characterFlyableShipHook)
	case boil.AfterSelectHook:
		characterFlyableShipAfterSelectHooks = append(characterFlyableShipAfterSelectHooks, characterFlyableShipHook)
	case boil.AfterUpdateHook:
		characterFlyableShipAfterUpdateHooks = append(characterFlyableShipAfterUpdateHooks, characterFlyableShipHook)
	case boil.AfterDeleteHook:
		characterFlyableShipAfterDeleteHooks = append(characterFlyableShipAfterDeleteHooks, characterFlyableShipHook)
	case boil.AfterUpsertHook:
		characterFlyableShipAfterUpsertHooks = append(characterFlyableShipAfterUpsertHooks, characterFlyableShipHook)
	}
}

// OneG returns a single characterFlyableShip record from the query using the global executor.
func (q characterFlyableShipQuery) OneG(ctx context.Context) (*CharacterFlyableShip, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single characterFlyableShip record from the query.
func (q characterFlyableShipQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CharacterFlyableShip, error) {
	o := &CharacterFlyableShip{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: failed to execute a one query for character_flyable_ships")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all CharacterFlyableShip records from the query using the global executor.
func (q characterFlyableShipQuery) AllG(ctx context.Context) (CharacterFlyableShipSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all CharacterFlyableShip records from the query.
func (q characterFlyableShipQuery) All(ctx context.Context, exec boil.ContextExecutor) (CharacterFlyableShipSlice, error) {
	var o []*CharacterFlyableShip

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "boiler: failed to assign all query results to CharacterFlyableShip slice")
	}

	if len(characterFlyableShipAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all CharacterFlyableShip records in the query, and panics on error.
func (q characterFlyableShipQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all CharacterFlyableShip records in the query.
func (q characterFlyableShipQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to count character_flyable_ships rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q characterFlyableShipQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q characterFlyableShipQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "boiler: failed to check if character_flyable_ships exists")
	}

	return count > 0, nil
}

// Character pointed to by the foreign key.
func (o *CharacterFlyableShip) Character(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`character_id` = ?", o.CharacterID),
	}

	queryMods = append(queryMods, mods...)

	query := Users(queryMods...)
	queries.SetFrom(query.Query, "`users`")

	return query
}

// ShipType pointed to by the foreign key.
func (o *CharacterFlyableShip) ShipType(mods ...qm.QueryMod) typeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`id` = ?", o.ShipTypeID),
	}

	queryMods = append(queryMods, mods...)

	query := Types(queryMods...)
	queries.SetFrom(query.Query, "`types`")

	return query
}

// LoadCharacter allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (characterFlyableShipL) LoadCharacter(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCharacterFlyableShip interface{}, mods queries.Applicator) error {
	var slice []*CharacterFlyableShip
	var object *CharacterFlyableShip

	if singular {
		object = maybeCharacterFlyableShip.(*CharacterFlyableShip)
	} else {
		slice = *maybeCharacterFlyableShip.(*[]*CharacterFlyableShip)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &characterFlyableShipR{}
		}
		args = append(args, object.CharacterID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &characterFlyableShipR{}
			}

			for _, a := range args {
				if a == obj.CharacterID {
					continue Outer
				}
			}

			args = append(args, obj.CharacterID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.character_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(characterFlyableShipAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Character = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.CharacterCharacterFlyableShips = append(foreign.R.CharacterCharacterFlyableShips, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CharacterID == foreign.CharacterID {
				local.R.Character = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.CharacterCharacterFlyableShips = append(foreign.R.CharacterCharacterFlyableShips, local)
				break
			}
		}
	}

	return nil
}

// LoadShipType allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (characterFlyableShipL) LoadShipType(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCharacterFlyableShip interface{}, mods queries.Applicator) error {
	var slice []*CharacterFlyableShip
	var object *CharacterFlyableShip

	if singular {
		object = maybeCharacterFlyableShip.(*CharacterFlyableShip)
	} else {
		slice = *maybeCharacterFlyableShip.(*[]*CharacterFlyableShip)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &characterFlyableShipR{}
		}
		args = append(args, object.ShipTypeID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &characterFlyableShipR{}
			}

			for _, a := range args {
				if a == obj.ShipTypeID {
					continue Outer
				}
			}

			args = append(args, obj.ShipTypeID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`types`),
		qm.WhereIn(`types.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Type")
	}

	var resultSlice []*Type
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Type")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for types")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for types")
	}

	if len(characterFlyableShipAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ShipType = foreign
		if foreign.R == nil {
			foreign.R = &typeR{}
		}
		foreign.R.ShipTypeCharacterFlyableShips = append(foreign.R.ShipTypeCharacterFlyableShips, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ShipTypeID == foreign.ID {
				local.R.ShipType = foreign
				if foreign.R == nil {
					foreign.R = &typeR{}
				}
				foreign.R.ShipTypeCharacterFlyableShips = append(foreign.R.ShipTypeCharacterFlyableShips, local)
				break
			}
		}
	}

	return nil
}

// SetCharacterG of the characterFlyableShip to the related item.
// Sets o.R.Character to related.
// Adds o to related.R.CharacterCharacterFlyableShips.
// Uses the global database handle.
func (o *CharacterFlyableShip) SetCharacterG(ctx context.Context, insert bool, related *User) error {
	return o.SetCharacter(ctx, boil.GetContextDB(), insert, related)
}

// SetCharacter of the characterFlyableShip to the related item.
// Sets o.R.Character to related.
// Adds o to related.R.CharacterCharacterFlyableShips.
func (o *CharacterFlyableShip) SetCharacter(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `character_flyable_ships` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"character_id"}),
		strmangle.WhereClause("`", "`", 0, characterFlyableShipPrimaryKeyColumns),
	)
	values := []interface{}{related.CharacterID, o.CharacterID, o.ShipTypeID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CharacterID = related.CharacterID
	if o.R == nil {
		o.R = &characterFlyableShipR{
			Character: related,
		}
	} else {
		o.R.Character = related
	}

	if related.R == nil {
		related.R = &userR{
			CharacterCharacterFlyableShips: CharacterFlyableShipSlice{o},
		}
	} else {
		related.R.CharacterCharacterFlyableShips = append(related.R.CharacterCharacterFlyableShips, o)
	}

	return nil
}

// SetShipTypeG of the characterFlyableShip to the related item.
// Sets o.R.ShipType to related.
// Adds o to related.R.ShipTypeCharacterFlyableShips.
// Uses the global database handle.
func (o *CharacterFlyableShip) SetShipTypeG(ctx context.Context, insert bool, related *Type) error {
	return o.SetShipType(ctx, boil.GetContextDB(), insert, related)
}

// SetShipType of the characterFlyableShip to the related item.
// Sets o.R.ShipType to related.
// Adds o to related.R.ShipTypeCharacterFlyableShips.
func (o *CharacterFlyableShip) SetShipType(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Type) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `character_flyable_ships` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ship_type_id"}),
		strmangle.WhereClause("`", "`", 0, characterFlyableShipPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.CharacterID, o.ShipTypeID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ShipTypeID = related.ID
	if o.R == nil {
		o.R = &characterFlyableShipR{
			ShipType: related,
		}
	} else {
		o.R.ShipType = related
	}

	if related.R == nil {
		related.R = &typeR{
			ShipTypeCharacterFlyableShips: CharacterFlyableShipSlice{o},
		}
	} else {
		related.R.ShipTypeCharacterFlyableShips = append(related.R.ShipTypeCharacterFlyableShips, o)
	}

	return nil
}

// CharacterFlyableShips retrieves all the records using an executor.
func CharacterFlyableShips(mods ...qm.QueryMod) characterFlyableShipQuery {
	mods = append(mods, qm.From("`character_flyable_ships`"))
	return characterFlyableShipQuery{NewQuery(mods...)}
}

// FindCharacterFlyableShipG retrieves a single record by ID.
func FindCharacterFlyableShipG(ctx context.Context, characterID uint64, shipTypeID uint, selectCols ...string) (*CharacterFlyableShip, error) {
	return FindCharacterFlyableShip(ctx, boil.GetContextDB(), characterID, shipTypeID, selectCols...)
}

// FindCharacterFlyableShip retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCharacterFlyableShip(ctx context.Context, exec boil.ContextExecutor, characterID uint64, shipTypeID uint, selectCols ...string) (*CharacterFlyableShip, error) {
	characterFlyableShipObj := &CharacterFlyableShip{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `character_flyable_ships` where `character_id`=? AND `ship_type_id`=?", sel,
	)

	q := queries.Raw(query, characterID, shipTypeID)

	err := q.Bind(ctx, exec, characterFlyableShipObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: unable to select from character_flyable_ships")
	}

	if err = characterFlyableShipObj.doAfterSelectHooks(ctx, exec); err != nil {
		return characterFlyableShipObj, err
	}

	return characterFlyableShipObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *CharacterFlyableShip) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CharacterFlyableShip) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no character_flyable_ships provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(characterFlyableShipColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	characterFlyableShipInsertCacheMut.RLock()
	cache, cached := characterFlyableShipInsertCache[key]
	characterFlyableShipInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			characterFlyableShipAllColumns,
			characterFlyableShipColumnsWithDefault,
			characterFlyableShipColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(characterFlyableShipType, characterFlyableShipMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(characterFlyableShipType, characterFlyableShipMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `character_flyable_ships` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `character_flyable_ships` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `character_flyable_ships` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, characterFlyableShipPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "boiler: unable to insert into character_flyable_ships")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CharacterID,
		o.ShipTypeID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to populate default values for character_flyable_ships")
	}

CacheNoHooks:
	if !cached {
		characterFlyableShipInsertCacheMut.Lock()
		characterFlyableShipInsertCache[key] = cache
		characterFlyableShipInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single CharacterFlyableShip record using the global executor.
// See Update for more documentation.
func (o *CharacterFlyableShip) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the CharacterFlyableShip.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CharacterFlyableShip) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	characterFlyableShipUpdateCacheMut.RLock()
	cache, cached := characterFlyableShipUpdateCache[key]
	characterFlyableShipUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			characterFlyableShipAllColumns,
			characterFlyableShipPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("boiler: unable to update character_flyable_ships, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `character_flyable_ships` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, characterFlyableShipPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(characterFlyableShipType, characterFlyableShipMapping, append(wl, characterFlyableShipPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update character_flyable_ships row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by update for character_flyable_ships")
	}

	if !cached {
		characterFlyableShipUpdateCacheMut.Lock()
		characterFlyableShipUpdateCache[key] = cache
		characterFlyableShipUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q characterFlyableShipQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q characterFlyableShipQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all for character_flyable_ships")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected for character_flyable_ships")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o CharacterFlyableShipSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CharacterFlyableShipSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("boiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), characterFlyableShipPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `character_flyable_ships` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, characterFlyableShipPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all in characterFlyableShip slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected all in update all characterFlyableShip")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *CharacterFlyableShip) UpsertG(ctx context.Context, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateColumns, insertColumns)
}

var mySQLCharacterFlyableShipUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CharacterFlyableShip) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no character_flyable_ships provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(characterFlyableShipColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCharacterFlyableShipUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	characterFlyableShipUpsertCacheMut.RLock()
	cache, cached := characterFlyableShipUpsertCache[key]
	characterFlyableShipUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			characterFlyableShipAllColumns,
			characterFlyableShipColumnsWithDefault,
			characterFlyableShipColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			characterFlyableShipAllColumns,
			characterFlyableShipPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("boiler: unable to upsert character_flyable_ships, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`character_flyable_ships`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `character_flyable_ships` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(characterFlyableShipType, characterFlyableShipMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(characterFlyableShipType, characterFlyableShipMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "boiler: unable to upsert for character_flyable_ships")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(characterFlyableShipType, characterFlyableShipMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to retrieve unique values for character_flyable_ships")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to populate default values for character_flyable_ships")
	}

CacheNoHooks:
	if !cached {
		characterFlyableShipUpsertCacheMut.Lock()
		characterFlyableShipUpsertCache[key] = cache
		characterFlyableShipUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single CharacterFlyableShip record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *CharacterFlyableShip) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single CharacterFlyableShip record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CharacterFlyableShip) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("boiler: no CharacterFlyableShip provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), characterFlyableShipPrimaryKeyMapping)
	sql := "DELETE FROM `character_flyable_ships` WHERE `character_id`=? AND `ship_type_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete from character_flyable_ships")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by delete for character_flyable_ships")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q characterFlyableShipQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q characterFlyableShipQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("boiler: no characterFlyableShipQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from character_flyable_ships")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for character_flyable_ships")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o CharacterFlyableShipSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CharacterFlyableShipSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(characterFlyableShipBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), characterFlyableShipPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `character_flyable_ships` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, characterFlyableShipPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from characterFlyableShip slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for character_flyable_ships")
	}

	if len(characterFlyableShipAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *CharacterFlyableShip) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("boiler: no CharacterFlyableShip provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CharacterFlyableShip) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCharacterFlyableShip(ctx, exec, o.CharacterID, o.ShipTypeID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CharacterFlyableShipSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("boiler: empty CharacterFlyableShipSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CharacterFlyableShipSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CharacterFlyableShipSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), characterFlyableShipPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `character_flyable_ships`.* FROM `character_flyable_ships` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, characterFlyableShipPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to reload all in CharacterFlyableShipSlice")
	}

	*o = slice

	return nil
}

// CharacterFlyableShipExistsG checks if the CharacterFlyableShip row exists.
func CharacterFlyableShipExistsG(ctx context.Context, characterID uint64, shipTypeID uint) (bool, error) {
	return CharacterFlyableShipExists(ctx, boil.GetContextDB(), characterID, shipTypeID)
}

// CharacterFlyableShipExists checks if the CharacterFlyableShip row exists.
func CharacterFlyableShipExists(ctx context.Context, exec boil.ContextExecutor, characterID uint64, shipTypeID uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `character_flyable_ships` where `character_id`=? AND `ship_type_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, characterID, shipTypeID)
	}
	row := exec.QueryRowContext(ctx, sql, characterID, shipTypeID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "boiler: unable to check if character_flyable_ships exists")
	}

	return exists, nil
}
