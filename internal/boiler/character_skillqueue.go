// Code generated by SQLBoiler 4.8.3 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package boiler

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CharacterSkillqueue is an object representing the database table.
type CharacterSkillqueue struct {
	CharacterID     uint64    `boil:"character_id" json:"character_id" toml:"character_id" yaml:"character_id"`
	QueuePosition   uint8     `boil:"queue_position" json:"queue_position" toml:"queue_position" yaml:"queue_position"`
	SkillID         uint      `boil:"skill_id" json:"skill_id" toml:"skill_id" yaml:"skill_id"`
	StartDate       null.Time `boil:"start_date" json:"start_date,omitempty" toml:"start_date" yaml:"start_date,omitempty"`
	FinishDate      null.Time `boil:"finish_date" json:"finish_date,omitempty" toml:"finish_date" yaml:"finish_date,omitempty"`
	FinishedLevel   uint8     `boil:"finished_level" json:"finished_level" toml:"finished_level" yaml:"finished_level"`
	TrainingStartSP null.Uint `boil:"training_start_sp" json:"training_start_sp,omitempty" toml:"training_start_sp" yaml:"training_start_sp,omitempty"`
	LevelStartSP    null.Uint `boil:"level_start_sp" json:"level_start_sp,omitempty" toml:"level_start_sp" yaml:"level_start_sp,omitempty"`
	LevelEndSP      null.Uint `boil:"level_end_sp" json:"level_end_sp,omitempty" toml:"level_end_sp" yaml:"level_end_sp,omitempty"`
	CreatedAt       time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *characterSkillqueueR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L characterSkillqueueL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CharacterSkillqueueColumns = struct {
	CharacterID     string
	QueuePosition   string
	SkillID         string
	StartDate       string
	FinishDate      string
	FinishedLevel   string
	TrainingStartSP string
	LevelStartSP    string
	LevelEndSP      string
	CreatedAt       string
}{
	CharacterID:     "character_id",
	QueuePosition:   "queue_position",
	SkillID:         "skill_id",
	StartDate:       "start_date",
	FinishDate:      "finish_date",
	FinishedLevel:   "finished_level",
	TrainingStartSP: "training_start_sp",
	LevelStartSP:    "level_start_sp",
	LevelEndSP:      "level_end_sp",
	CreatedAt:       "created_at",
}

var CharacterSkillqueueTableColumns = struct {
	CharacterID     string
	QueuePosition   string
	SkillID         string
	StartDate       string
	FinishDate      string
	FinishedLevel   string
	TrainingStartSP string
	LevelStartSP    string
	LevelEndSP      string
	CreatedAt       string
}{
	CharacterID:     "character_skillqueue.character_id",
	QueuePosition:   "character_skillqueue.queue_position",
	SkillID:         "character_skillqueue.skill_id",
	StartDate:       "character_skillqueue.start_date",
	FinishDate:      "character_skillqueue.finish_date",
	FinishedLevel:   "character_skillqueue.finished_level",
	TrainingStartSP: "character_skillqueue.training_start_sp",
	LevelStartSP:    "character_skillqueue.level_start_sp",
	LevelEndSP:      "character_skillqueue.level_end_sp",
	CreatedAt:       "character_skillqueue.created_at",
}

// Generated where

var CharacterSkillqueueWhere = struct {
	CharacterID     whereHelperuint64
	QueuePosition   whereHelperuint8
	SkillID         whereHelperuint
	StartDate       whereHelpernull_Time
	FinishDate      whereHelpernull_Time
	FinishedLevel   whereHelperuint8
	TrainingStartSP whereHelpernull_Uint
	LevelStartSP    whereHelpernull_Uint
	LevelEndSP      whereHelpernull_Uint
	CreatedAt       whereHelpertime_Time
}{
	CharacterID:     whereHelperuint64{field: "`character_skillqueue`.`character_id`"},
	QueuePosition:   whereHelperuint8{field: "`character_skillqueue`.`queue_position`"},
	SkillID:         whereHelperuint{field: "`character_skillqueue`.`skill_id`"},
	StartDate:       whereHelpernull_Time{field: "`character_skillqueue`.`start_date`"},
	FinishDate:      whereHelpernull_Time{field: "`character_skillqueue`.`finish_date`"},
	FinishedLevel:   whereHelperuint8{field: "`character_skillqueue`.`finished_level`"},
	TrainingStartSP: whereHelpernull_Uint{field: "`character_skillqueue`.`training_start_sp`"},
	LevelStartSP:    whereHelpernull_Uint{field: "`character_skillqueue`.`level_start_sp`"},
	LevelEndSP:      whereHelpernull_Uint{field: "`character_skillqueue`.`level_end_sp`"},
	CreatedAt:       whereHelpertime_Time{field: "`character_skillqueue`.`created_at`"},
}

// CharacterSkillqueueRels is where relationship names are stored.
var CharacterSkillqueueRels = struct {
	Character string
}{
	Character: "Character",
}

// characterSkillqueueR is where relationships are stored.
type characterSkillqueueR struct {
	Character *User `boil:"Character" json:"Character" toml:"Character" yaml:"Character"`
}

// NewStruct creates a new relationship struct
func (*characterSkillqueueR) NewStruct() *characterSkillqueueR {
	return &characterSkillqueueR{}
}

// characterSkillqueueL is where Load methods for each relationship are stored.
type characterSkillqueueL struct{}

var (
	characterSkillqueueAllColumns            = []string{"character_id", "queue_position", "skill_id", "start_date", "finish_date", "finished_level", "training_start_sp", "level_start_sp", "level_end_sp", "created_at"}
	characterSkillqueueColumnsWithoutDefault = []string{"character_id", "queue_position", "skill_id", "start_date", "finish_date", "finished_level", "training_start_sp", "level_start_sp", "level_end_sp", "created_at"}
	characterSkillqueueColumnsWithDefault    = []string{}
	characterSkillqueuePrimaryKeyColumns     = []string{"character_id", "queue_position"}
)

type (
	// CharacterSkillqueueSlice is an alias for a slice of pointers to CharacterSkillqueue.
	// This should almost always be used instead of []CharacterSkillqueue.
	CharacterSkillqueueSlice []*CharacterSkillqueue
	// CharacterSkillqueueHook is the signature for custom CharacterSkillqueue hook methods
	CharacterSkillqueueHook func(context.Context, boil.ContextExecutor, *CharacterSkillqueue) error

	characterSkillqueueQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	characterSkillqueueType                 = reflect.TypeOf(&CharacterSkillqueue{})
	characterSkillqueueMapping              = queries.MakeStructMapping(characterSkillqueueType)
	characterSkillqueuePrimaryKeyMapping, _ = queries.BindMapping(characterSkillqueueType, characterSkillqueueMapping, characterSkillqueuePrimaryKeyColumns)
	characterSkillqueueInsertCacheMut       sync.RWMutex
	characterSkillqueueInsertCache          = make(map[string]insertCache)
	characterSkillqueueUpdateCacheMut       sync.RWMutex
	characterSkillqueueUpdateCache          = make(map[string]updateCache)
	characterSkillqueueUpsertCacheMut       sync.RWMutex
	characterSkillqueueUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var characterSkillqueueBeforeInsertHooks []CharacterSkillqueueHook
var characterSkillqueueBeforeUpdateHooks []CharacterSkillqueueHook
var characterSkillqueueBeforeDeleteHooks []CharacterSkillqueueHook
var characterSkillqueueBeforeUpsertHooks []CharacterSkillqueueHook

var characterSkillqueueAfterInsertHooks []CharacterSkillqueueHook
var characterSkillqueueAfterSelectHooks []CharacterSkillqueueHook
var characterSkillqueueAfterUpdateHooks []CharacterSkillqueueHook
var characterSkillqueueAfterDeleteHooks []CharacterSkillqueueHook
var characterSkillqueueAfterUpsertHooks []CharacterSkillqueueHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CharacterSkillqueue) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterSkillqueueBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CharacterSkillqueue) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterSkillqueueBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CharacterSkillqueue) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterSkillqueueBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CharacterSkillqueue) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterSkillqueueBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CharacterSkillqueue) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterSkillqueueAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CharacterSkillqueue) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterSkillqueueAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CharacterSkillqueue) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterSkillqueueAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CharacterSkillqueue) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterSkillqueueAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CharacterSkillqueue) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range characterSkillqueueAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCharacterSkillqueueHook registers your hook function for all future operations.
func AddCharacterSkillqueueHook(hookPoint boil.HookPoint, characterSkillqueueHook CharacterSkillqueueHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		characterSkillqueueBeforeInsertHooks = append(characterSkillqueueBeforeInsertHooks, characterSkillqueueHook)
	case boil.BeforeUpdateHook:
		characterSkillqueueBeforeUpdateHooks = append(characterSkillqueueBeforeUpdateHooks, characterSkillqueueHook)
	case boil.BeforeDeleteHook:
		characterSkillqueueBeforeDeleteHooks = append(characterSkillqueueBeforeDeleteHooks, characterSkillqueueHook)
	case boil.BeforeUpsertHook:
		characterSkillqueueBeforeUpsertHooks = append(characterSkillqueueBeforeUpsertHooks, characterSkillqueueHook)
	case boil.AfterInsertHook:
		characterSkillqueueAfterInsertHooks = append(characterSkillqueueAfterInsertHooks, characterSkillqueueHook)
	case boil.AfterSelectHook:
		characterSkillqueueAfterSelectHooks = append(characterSkillqueueAfterSelectHooks, characterSkillqueueHook)
	case boil.AfterUpdateHook:
		characterSkillqueueAfterUpdateHooks = append(characterSkillqueueAfterUpdateHooks, characterSkillqueueHook)
	case boil.AfterDeleteHook:
		characterSkillqueueAfterDeleteHooks = append(characterSkillqueueAfterDeleteHooks, characterSkillqueueHook)
	case boil.AfterUpsertHook:
		characterSkillqueueAfterUpsertHooks = append(characterSkillqueueAfterUpsertHooks, characterSkillqueueHook)
	}
}

// OneG returns a single characterSkillqueue record from the query using the global executor.
func (q characterSkillqueueQuery) OneG(ctx context.Context) (*CharacterSkillqueue, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single characterSkillqueue record from the query.
func (q characterSkillqueueQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CharacterSkillqueue, error) {
	o := &CharacterSkillqueue{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: failed to execute a one query for character_skillqueue")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all CharacterSkillqueue records from the query using the global executor.
func (q characterSkillqueueQuery) AllG(ctx context.Context) (CharacterSkillqueueSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all CharacterSkillqueue records from the query.
func (q characterSkillqueueQuery) All(ctx context.Context, exec boil.ContextExecutor) (CharacterSkillqueueSlice, error) {
	var o []*CharacterSkillqueue

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "boiler: failed to assign all query results to CharacterSkillqueue slice")
	}

	if len(characterSkillqueueAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all CharacterSkillqueue records in the query, and panics on error.
func (q characterSkillqueueQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all CharacterSkillqueue records in the query.
func (q characterSkillqueueQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to count character_skillqueue rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q characterSkillqueueQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q characterSkillqueueQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "boiler: failed to check if character_skillqueue exists")
	}

	return count > 0, nil
}

// Character pointed to by the foreign key.
func (o *CharacterSkillqueue) Character(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`character_id` = ?", o.CharacterID),
	}

	queryMods = append(queryMods, mods...)

	query := Users(queryMods...)
	queries.SetFrom(query.Query, "`users`")

	return query
}

// LoadCharacter allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (characterSkillqueueL) LoadCharacter(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCharacterSkillqueue interface{}, mods queries.Applicator) error {
	var slice []*CharacterSkillqueue
	var object *CharacterSkillqueue

	if singular {
		object = maybeCharacterSkillqueue.(*CharacterSkillqueue)
	} else {
		slice = *maybeCharacterSkillqueue.(*[]*CharacterSkillqueue)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &characterSkillqueueR{}
		}
		args = append(args, object.CharacterID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &characterSkillqueueR{}
			}

			for _, a := range args {
				if a == obj.CharacterID {
					continue Outer
				}
			}

			args = append(args, obj.CharacterID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.character_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(characterSkillqueueAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Character = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.CharacterCharacterSkillqueues = append(foreign.R.CharacterCharacterSkillqueues, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CharacterID == foreign.CharacterID {
				local.R.Character = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.CharacterCharacterSkillqueues = append(foreign.R.CharacterCharacterSkillqueues, local)
				break
			}
		}
	}

	return nil
}

// SetCharacterG of the characterSkillqueue to the related item.
// Sets o.R.Character to related.
// Adds o to related.R.CharacterCharacterSkillqueues.
// Uses the global database handle.
func (o *CharacterSkillqueue) SetCharacterG(ctx context.Context, insert bool, related *User) error {
	return o.SetCharacter(ctx, boil.GetContextDB(), insert, related)
}

// SetCharacter of the characterSkillqueue to the related item.
// Sets o.R.Character to related.
// Adds o to related.R.CharacterCharacterSkillqueues.
func (o *CharacterSkillqueue) SetCharacter(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `character_skillqueue` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"character_id"}),
		strmangle.WhereClause("`", "`", 0, characterSkillqueuePrimaryKeyColumns),
	)
	values := []interface{}{related.CharacterID, o.CharacterID, o.QueuePosition}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CharacterID = related.CharacterID
	if o.R == nil {
		o.R = &characterSkillqueueR{
			Character: related,
		}
	} else {
		o.R.Character = related
	}

	if related.R == nil {
		related.R = &userR{
			CharacterCharacterSkillqueues: CharacterSkillqueueSlice{o},
		}
	} else {
		related.R.CharacterCharacterSkillqueues = append(related.R.CharacterCharacterSkillqueues, o)
	}

	return nil
}

// CharacterSkillqueues retrieves all the records using an executor.
func CharacterSkillqueues(mods ...qm.QueryMod) characterSkillqueueQuery {
	mods = append(mods, qm.From("`character_skillqueue`"))
	return characterSkillqueueQuery{NewQuery(mods...)}
}

// FindCharacterSkillqueueG retrieves a single record by ID.
func FindCharacterSkillqueueG(ctx context.Context, characterID uint64, queuePosition uint8, selectCols ...string) (*CharacterSkillqueue, error) {
	return FindCharacterSkillqueue(ctx, boil.GetContextDB(), characterID, queuePosition, selectCols...)
}

// FindCharacterSkillqueue retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCharacterSkillqueue(ctx context.Context, exec boil.ContextExecutor, characterID uint64, queuePosition uint8, selectCols ...string) (*CharacterSkillqueue, error) {
	characterSkillqueueObj := &CharacterSkillqueue{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `character_skillqueue` where `character_id`=? AND `queue_position`=?", sel,
	)

	q := queries.Raw(query, characterID, queuePosition)

	err := q.Bind(ctx, exec, characterSkillqueueObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: unable to select from character_skillqueue")
	}

	if err = characterSkillqueueObj.doAfterSelectHooks(ctx, exec); err != nil {
		return characterSkillqueueObj, err
	}

	return characterSkillqueueObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *CharacterSkillqueue) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CharacterSkillqueue) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no character_skillqueue provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(characterSkillqueueColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	characterSkillqueueInsertCacheMut.RLock()
	cache, cached := characterSkillqueueInsertCache[key]
	characterSkillqueueInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			characterSkillqueueAllColumns,
			characterSkillqueueColumnsWithDefault,
			characterSkillqueueColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(characterSkillqueueType, characterSkillqueueMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(characterSkillqueueType, characterSkillqueueMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `character_skillqueue` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `character_skillqueue` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `character_skillqueue` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, characterSkillqueuePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "boiler: unable to insert into character_skillqueue")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.CharacterID,
		o.QueuePosition,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to populate default values for character_skillqueue")
	}

CacheNoHooks:
	if !cached {
		characterSkillqueueInsertCacheMut.Lock()
		characterSkillqueueInsertCache[key] = cache
		characterSkillqueueInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single CharacterSkillqueue record using the global executor.
// See Update for more documentation.
func (o *CharacterSkillqueue) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the CharacterSkillqueue.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CharacterSkillqueue) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	characterSkillqueueUpdateCacheMut.RLock()
	cache, cached := characterSkillqueueUpdateCache[key]
	characterSkillqueueUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			characterSkillqueueAllColumns,
			characterSkillqueuePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("boiler: unable to update character_skillqueue, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `character_skillqueue` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, characterSkillqueuePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(characterSkillqueueType, characterSkillqueueMapping, append(wl, characterSkillqueuePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update character_skillqueue row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by update for character_skillqueue")
	}

	if !cached {
		characterSkillqueueUpdateCacheMut.Lock()
		characterSkillqueueUpdateCache[key] = cache
		characterSkillqueueUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q characterSkillqueueQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q characterSkillqueueQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all for character_skillqueue")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected for character_skillqueue")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o CharacterSkillqueueSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CharacterSkillqueueSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("boiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), characterSkillqueuePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `character_skillqueue` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, characterSkillqueuePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all in characterSkillqueue slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected all in update all characterSkillqueue")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *CharacterSkillqueue) UpsertG(ctx context.Context, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateColumns, insertColumns)
}

var mySQLCharacterSkillqueueUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CharacterSkillqueue) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no character_skillqueue provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(characterSkillqueueColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCharacterSkillqueueUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	characterSkillqueueUpsertCacheMut.RLock()
	cache, cached := characterSkillqueueUpsertCache[key]
	characterSkillqueueUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			characterSkillqueueAllColumns,
			characterSkillqueueColumnsWithDefault,
			characterSkillqueueColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			characterSkillqueueAllColumns,
			characterSkillqueuePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("boiler: unable to upsert character_skillqueue, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`character_skillqueue`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `character_skillqueue` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(characterSkillqueueType, characterSkillqueueMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(characterSkillqueueType, characterSkillqueueMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "boiler: unable to upsert for character_skillqueue")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(characterSkillqueueType, characterSkillqueueMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to retrieve unique values for character_skillqueue")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to populate default values for character_skillqueue")
	}

CacheNoHooks:
	if !cached {
		characterSkillqueueUpsertCacheMut.Lock()
		characterSkillqueueUpsertCache[key] = cache
		characterSkillqueueUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single CharacterSkillqueue record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *CharacterSkillqueue) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single CharacterSkillqueue record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CharacterSkillqueue) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("boiler: no CharacterSkillqueue provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), characterSkillqueuePrimaryKeyMapping)
	sql := "DELETE FROM `character_skillqueue` WHERE `character_id`=? AND `queue_position`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete from character_skillqueue")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by delete for character_skillqueue")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q characterSkillqueueQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q characterSkillqueueQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("boiler: no characterSkillqueueQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from character_skillqueue")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for character_skillqueue")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o CharacterSkillqueueSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CharacterSkillqueueSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(characterSkillqueueBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), characterSkillqueuePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `character_skillqueue` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, characterSkillqueuePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from characterSkillqueue slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for character_skillqueue")
	}

	if len(characterSkillqueueAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *CharacterSkillqueue) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("boiler: no CharacterSkillqueue provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CharacterSkillqueue) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCharacterSkillqueue(ctx, exec, o.CharacterID, o.QueuePosition)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CharacterSkillqueueSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("boiler: empty CharacterSkillqueueSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CharacterSkillqueueSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CharacterSkillqueueSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), characterSkillqueuePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `character_skillqueue`.* FROM `character_skillqueue` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, characterSkillqueuePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to reload all in CharacterSkillqueueSlice")
	}

	*o = slice

	return nil
}

// CharacterSkillqueueExistsG checks if the CharacterSkillqueue row exists.
func CharacterSkillqueueExistsG(ctx context.Context, characterID uint64, queuePosition uint8) (bool, error) {
	return CharacterSkillqueueExists(ctx, boil.GetContextDB(), characterID, queuePosition)
}

// CharacterSkillqueueExists checks if the CharacterSkillqueue row exists.
func CharacterSkillqueueExists(ctx context.Context, exec boil.ContextExecutor, characterID uint64, queuePosition uint8) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `character_skillqueue` where `character_id`=? AND `queue_position`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, characterID, queuePosition)
	}
	row := exec.QueryRowContext(ctx, sql, characterID, queuePosition)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "boiler: unable to check if character_skillqueue exists")
	}

	return exists, nil
}
