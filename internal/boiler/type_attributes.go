// Code generated by SQLBoiler 4.8.3 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package boiler

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TypeAttribute is an object representing the database table.
type TypeAttribute struct {
	TypeID      uint      `boil:"type_id" json:"type_id" toml:"type_id" yaml:"type_id"`
	AttributeID uint      `boil:"attribute_id" json:"attribute_id" toml:"attribute_id" yaml:"attribute_id"`
	Value       float32   `boil:"value" json:"value" toml:"value" yaml:"value"`
	CreatedAt   time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *typeAttributeR `boil:"r" json:"r" toml:"r" yaml:"r"`
	L typeAttributeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TypeAttributeColumns = struct {
	TypeID      string
	AttributeID string
	Value       string
	CreatedAt   string
}{
	TypeID:      "type_id",
	AttributeID: "attribute_id",
	Value:       "value",
	CreatedAt:   "created_at",
}

var TypeAttributeTableColumns = struct {
	TypeID      string
	AttributeID string
	Value       string
	CreatedAt   string
}{
	TypeID:      "type_attributes.type_id",
	AttributeID: "type_attributes.attribute_id",
	Value:       "type_attributes.value",
	CreatedAt:   "type_attributes.created_at",
}

// Generated where

var TypeAttributeWhere = struct {
	TypeID      whereHelperuint
	AttributeID whereHelperuint
	Value       whereHelperfloat32
	CreatedAt   whereHelpertime_Time
}{
	TypeID:      whereHelperuint{field: "`type_attributes`.`type_id`"},
	AttributeID: whereHelperuint{field: "`type_attributes`.`attribute_id`"},
	Value:       whereHelperfloat32{field: "`type_attributes`.`value`"},
	CreatedAt:   whereHelpertime_Time{field: "`type_attributes`.`created_at`"},
}

// TypeAttributeRels is where relationship names are stored.
var TypeAttributeRels = struct {
	Type string
}{
	Type: "Type",
}

// typeAttributeR is where relationships are stored.
type typeAttributeR struct {
	Type *Type `boil:"Type" json:"Type" toml:"Type" yaml:"Type"`
}

// NewStruct creates a new relationship struct
func (*typeAttributeR) NewStruct() *typeAttributeR {
	return &typeAttributeR{}
}

// typeAttributeL is where Load methods for each relationship are stored.
type typeAttributeL struct{}

var (
	typeAttributeAllColumns            = []string{"type_id", "attribute_id", "value", "created_at"}
	typeAttributeColumnsWithoutDefault = []string{"type_id", "attribute_id", "value", "created_at"}
	typeAttributeColumnsWithDefault    = []string{}
	typeAttributePrimaryKeyColumns     = []string{"type_id", "attribute_id"}
)

type (
	// TypeAttributeSlice is an alias for a slice of pointers to TypeAttribute.
	// This should almost always be used instead of []TypeAttribute.
	TypeAttributeSlice []*TypeAttribute
	// TypeAttributeHook is the signature for custom TypeAttribute hook methods
	TypeAttributeHook func(context.Context, boil.ContextExecutor, *TypeAttribute) error

	typeAttributeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	typeAttributeType                 = reflect.TypeOf(&TypeAttribute{})
	typeAttributeMapping              = queries.MakeStructMapping(typeAttributeType)
	typeAttributePrimaryKeyMapping, _ = queries.BindMapping(typeAttributeType, typeAttributeMapping, typeAttributePrimaryKeyColumns)
	typeAttributeInsertCacheMut       sync.RWMutex
	typeAttributeInsertCache          = make(map[string]insertCache)
	typeAttributeUpdateCacheMut       sync.RWMutex
	typeAttributeUpdateCache          = make(map[string]updateCache)
	typeAttributeUpsertCacheMut       sync.RWMutex
	typeAttributeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var typeAttributeBeforeInsertHooks []TypeAttributeHook
var typeAttributeBeforeUpdateHooks []TypeAttributeHook
var typeAttributeBeforeDeleteHooks []TypeAttributeHook
var typeAttributeBeforeUpsertHooks []TypeAttributeHook

var typeAttributeAfterInsertHooks []TypeAttributeHook
var typeAttributeAfterSelectHooks []TypeAttributeHook
var typeAttributeAfterUpdateHooks []TypeAttributeHook
var typeAttributeAfterDeleteHooks []TypeAttributeHook
var typeAttributeAfterUpsertHooks []TypeAttributeHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TypeAttribute) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range typeAttributeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TypeAttribute) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range typeAttributeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TypeAttribute) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range typeAttributeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TypeAttribute) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range typeAttributeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TypeAttribute) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range typeAttributeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TypeAttribute) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range typeAttributeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TypeAttribute) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range typeAttributeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TypeAttribute) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range typeAttributeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TypeAttribute) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range typeAttributeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTypeAttributeHook registers your hook function for all future operations.
func AddTypeAttributeHook(hookPoint boil.HookPoint, typeAttributeHook TypeAttributeHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		typeAttributeBeforeInsertHooks = append(typeAttributeBeforeInsertHooks, typeAttributeHook)
	case boil.BeforeUpdateHook:
		typeAttributeBeforeUpdateHooks = append(typeAttributeBeforeUpdateHooks, typeAttributeHook)
	case boil.BeforeDeleteHook:
		typeAttributeBeforeDeleteHooks = append(typeAttributeBeforeDeleteHooks, typeAttributeHook)
	case boil.BeforeUpsertHook:
		typeAttributeBeforeUpsertHooks = append(typeAttributeBeforeUpsertHooks, typeAttributeHook)
	case boil.AfterInsertHook:
		typeAttributeAfterInsertHooks = append(typeAttributeAfterInsertHooks, typeAttributeHook)
	case boil.AfterSelectHook:
		typeAttributeAfterSelectHooks = append(typeAttributeAfterSelectHooks, typeAttributeHook)
	case boil.AfterUpdateHook:
		typeAttributeAfterUpdateHooks = append(typeAttributeAfterUpdateHooks, typeAttributeHook)
	case boil.AfterDeleteHook:
		typeAttributeAfterDeleteHooks = append(typeAttributeAfterDeleteHooks, typeAttributeHook)
	case boil.AfterUpsertHook:
		typeAttributeAfterUpsertHooks = append(typeAttributeAfterUpsertHooks, typeAttributeHook)
	}
}

// OneG returns a single typeAttribute record from the query using the global executor.
func (q typeAttributeQuery) OneG(ctx context.Context) (*TypeAttribute, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single typeAttribute record from the query.
func (q typeAttributeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TypeAttribute, error) {
	o := &TypeAttribute{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: failed to execute a one query for type_attributes")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all TypeAttribute records from the query using the global executor.
func (q typeAttributeQuery) AllG(ctx context.Context) (TypeAttributeSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all TypeAttribute records from the query.
func (q typeAttributeQuery) All(ctx context.Context, exec boil.ContextExecutor) (TypeAttributeSlice, error) {
	var o []*TypeAttribute

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "boiler: failed to assign all query results to TypeAttribute slice")
	}

	if len(typeAttributeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all TypeAttribute records in the query, and panics on error.
func (q typeAttributeQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all TypeAttribute records in the query.
func (q typeAttributeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to count type_attributes rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q typeAttributeQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q typeAttributeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "boiler: failed to check if type_attributes exists")
	}

	return count > 0, nil
}

// Type pointed to by the foreign key.
func (o *TypeAttribute) Type(mods ...qm.QueryMod) typeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`id` = ?", o.TypeID),
	}

	queryMods = append(queryMods, mods...)

	query := Types(queryMods...)
	queries.SetFrom(query.Query, "`types`")

	return query
}

// LoadType allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (typeAttributeL) LoadType(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTypeAttribute interface{}, mods queries.Applicator) error {
	var slice []*TypeAttribute
	var object *TypeAttribute

	if singular {
		object = maybeTypeAttribute.(*TypeAttribute)
	} else {
		slice = *maybeTypeAttribute.(*[]*TypeAttribute)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &typeAttributeR{}
		}
		args = append(args, object.TypeID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &typeAttributeR{}
			}

			for _, a := range args {
				if a == obj.TypeID {
					continue Outer
				}
			}

			args = append(args, obj.TypeID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`types`),
		qm.WhereIn(`types.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Type")
	}

	var resultSlice []*Type
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Type")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for types")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for types")
	}

	if len(typeAttributeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Type = foreign
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TypeID == foreign.ID {
				local.R.Type = foreign
				break
			}
		}
	}

	return nil
}

// SetTypeG of the typeAttribute to the related item.
// Sets o.R.Type to related.
// Adds o to related.R.TypeAttributes.
// Uses the global database handle.
func (o *TypeAttribute) SetTypeG(ctx context.Context, insert bool, related *Type) error {
	return o.SetType(ctx, boil.GetContextDB(), insert, related)
}

// SetType of the typeAttribute to the related item.
// Sets o.R.Type to related.
// Adds o to related.R.TypeAttributes.
func (o *TypeAttribute) SetType(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Type) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `type_attributes` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"type_id"}),
		strmangle.WhereClause("`", "`", 0, typeAttributePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.TypeID, o.AttributeID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TypeID = related.ID
	if o.R == nil {
		o.R = &typeAttributeR{
			Type: related,
		}
	} else {
		o.R.Type = related
	}

	if related.R == nil {
		related.R = &typeR{
			TypeAttributes: TypeAttributeSlice{o},
		}
	} else {
		related.R.TypeAttributes = append(related.R.TypeAttributes, o)
	}

	return nil
}

// TypeAttributes retrieves all the records using an executor.
func TypeAttributes(mods ...qm.QueryMod) typeAttributeQuery {
	mods = append(mods, qm.From("`type_attributes`"))
	return typeAttributeQuery{NewQuery(mods...)}
}

// FindTypeAttributeG retrieves a single record by ID.
func FindTypeAttributeG(ctx context.Context, typeID uint, attributeID uint, selectCols ...string) (*TypeAttribute, error) {
	return FindTypeAttribute(ctx, boil.GetContextDB(), typeID, attributeID, selectCols...)
}

// FindTypeAttribute retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTypeAttribute(ctx context.Context, exec boil.ContextExecutor, typeID uint, attributeID uint, selectCols ...string) (*TypeAttribute, error) {
	typeAttributeObj := &TypeAttribute{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `type_attributes` where `type_id`=? AND `attribute_id`=?", sel,
	)

	q := queries.Raw(query, typeID, attributeID)

	err := q.Bind(ctx, exec, typeAttributeObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: unable to select from type_attributes")
	}

	if err = typeAttributeObj.doAfterSelectHooks(ctx, exec); err != nil {
		return typeAttributeObj, err
	}

	return typeAttributeObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TypeAttribute) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TypeAttribute) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no type_attributes provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(typeAttributeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	typeAttributeInsertCacheMut.RLock()
	cache, cached := typeAttributeInsertCache[key]
	typeAttributeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			typeAttributeAllColumns,
			typeAttributeColumnsWithDefault,
			typeAttributeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(typeAttributeType, typeAttributeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(typeAttributeType, typeAttributeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `type_attributes` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `type_attributes` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `type_attributes` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, typeAttributePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "boiler: unable to insert into type_attributes")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.TypeID,
		o.AttributeID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to populate default values for type_attributes")
	}

CacheNoHooks:
	if !cached {
		typeAttributeInsertCacheMut.Lock()
		typeAttributeInsertCache[key] = cache
		typeAttributeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single TypeAttribute record using the global executor.
// See Update for more documentation.
func (o *TypeAttribute) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the TypeAttribute.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TypeAttribute) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	typeAttributeUpdateCacheMut.RLock()
	cache, cached := typeAttributeUpdateCache[key]
	typeAttributeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			typeAttributeAllColumns,
			typeAttributePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("boiler: unable to update type_attributes, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `type_attributes` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, typeAttributePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(typeAttributeType, typeAttributeMapping, append(wl, typeAttributePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update type_attributes row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by update for type_attributes")
	}

	if !cached {
		typeAttributeUpdateCacheMut.Lock()
		typeAttributeUpdateCache[key] = cache
		typeAttributeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q typeAttributeQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q typeAttributeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all for type_attributes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected for type_attributes")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TypeAttributeSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TypeAttributeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("boiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), typeAttributePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `type_attributes` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, typeAttributePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all in typeAttribute slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected all in update all typeAttribute")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TypeAttribute) UpsertG(ctx context.Context, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateColumns, insertColumns)
}

var mySQLTypeAttributeUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TypeAttribute) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no type_attributes provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(typeAttributeColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTypeAttributeUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	typeAttributeUpsertCacheMut.RLock()
	cache, cached := typeAttributeUpsertCache[key]
	typeAttributeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			typeAttributeAllColumns,
			typeAttributeColumnsWithDefault,
			typeAttributeColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			typeAttributeAllColumns,
			typeAttributePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("boiler: unable to upsert type_attributes, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`type_attributes`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `type_attributes` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(typeAttributeType, typeAttributeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(typeAttributeType, typeAttributeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "boiler: unable to upsert for type_attributes")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(typeAttributeType, typeAttributeMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to retrieve unique values for type_attributes")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to populate default values for type_attributes")
	}

CacheNoHooks:
	if !cached {
		typeAttributeUpsertCacheMut.Lock()
		typeAttributeUpsertCache[key] = cache
		typeAttributeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single TypeAttribute record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TypeAttribute) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single TypeAttribute record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TypeAttribute) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("boiler: no TypeAttribute provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), typeAttributePrimaryKeyMapping)
	sql := "DELETE FROM `type_attributes` WHERE `type_id`=? AND `attribute_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete from type_attributes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by delete for type_attributes")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q typeAttributeQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q typeAttributeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("boiler: no typeAttributeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from type_attributes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for type_attributes")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TypeAttributeSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TypeAttributeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(typeAttributeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), typeAttributePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `type_attributes` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, typeAttributePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from typeAttribute slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for type_attributes")
	}

	if len(typeAttributeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TypeAttribute) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("boiler: no TypeAttribute provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TypeAttribute) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTypeAttribute(ctx, exec, o.TypeID, o.AttributeID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TypeAttributeSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("boiler: empty TypeAttributeSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TypeAttributeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TypeAttributeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), typeAttributePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `type_attributes`.* FROM `type_attributes` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, typeAttributePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to reload all in TypeAttributeSlice")
	}

	*o = slice

	return nil
}

// TypeAttributeExistsG checks if the TypeAttribute row exists.
func TypeAttributeExistsG(ctx context.Context, typeID uint, attributeID uint) (bool, error) {
	return TypeAttributeExists(ctx, boil.GetContextDB(), typeID, attributeID)
}

// TypeAttributeExists checks if the TypeAttribute row exists.
func TypeAttributeExists(ctx context.Context, exec boil.ContextExecutor, typeID uint, attributeID uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `type_attributes` where `type_id`=? AND `attribute_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, typeID, attributeID)
	}
	row := exec.QueryRowContext(ctx, sql, typeID, attributeID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "boiler: unable to check if type_attributes exists")
	}

	return exists, nil
}
