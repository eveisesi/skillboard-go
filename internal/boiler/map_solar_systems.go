// Code generated by SQLBoiler 4.8.3 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package boiler

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// MapSolarSystem is an object representing the database table.
type MapSolarSystem struct {
	ID              uint        `boil:"id" json:"id" toml:"id" yaml:"id"`
	Name            string      `boil:"name" json:"name" toml:"name" yaml:"name"`
	ConstellationID uint        `boil:"constellation_id" json:"constellation_id" toml:"constellation_id" yaml:"constellation_id"`
	SecurityClass   null.String `boil:"security_class" json:"security_class,omitempty" toml:"security_class" yaml:"security_class,omitempty"`
	SecurityStatus  float32     `boil:"security_status" json:"security_status" toml:"security_status" yaml:"security_status"`
	StarID          null.Uint   `boil:"star_id" json:"star_id,omitempty" toml:"star_id" yaml:"star_id,omitempty"`
	CreatedAt       time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt       time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *mapSolarSystemR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L mapSolarSystemL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MapSolarSystemColumns = struct {
	ID              string
	Name            string
	ConstellationID string
	SecurityClass   string
	SecurityStatus  string
	StarID          string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "id",
	Name:            "name",
	ConstellationID: "constellation_id",
	SecurityClass:   "security_class",
	SecurityStatus:  "security_status",
	StarID:          "star_id",
	CreatedAt:       "created_at",
	UpdatedAt:       "updated_at",
}

var MapSolarSystemTableColumns = struct {
	ID              string
	Name            string
	ConstellationID string
	SecurityClass   string
	SecurityStatus  string
	StarID          string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "map_solar_systems.id",
	Name:            "map_solar_systems.name",
	ConstellationID: "map_solar_systems.constellation_id",
	SecurityClass:   "map_solar_systems.security_class",
	SecurityStatus:  "map_solar_systems.security_status",
	StarID:          "map_solar_systems.star_id",
	CreatedAt:       "map_solar_systems.created_at",
	UpdatedAt:       "map_solar_systems.updated_at",
}

// Generated where

var MapSolarSystemWhere = struct {
	ID              whereHelperuint
	Name            whereHelperstring
	ConstellationID whereHelperuint
	SecurityClass   whereHelpernull_String
	SecurityStatus  whereHelperfloat32
	StarID          whereHelpernull_Uint
	CreatedAt       whereHelpertime_Time
	UpdatedAt       whereHelpertime_Time
}{
	ID:              whereHelperuint{field: "`map_solar_systems`.`id`"},
	Name:            whereHelperstring{field: "`map_solar_systems`.`name`"},
	ConstellationID: whereHelperuint{field: "`map_solar_systems`.`constellation_id`"},
	SecurityClass:   whereHelpernull_String{field: "`map_solar_systems`.`security_class`"},
	SecurityStatus:  whereHelperfloat32{field: "`map_solar_systems`.`security_status`"},
	StarID:          whereHelpernull_Uint{field: "`map_solar_systems`.`star_id`"},
	CreatedAt:       whereHelpertime_Time{field: "`map_solar_systems`.`created_at`"},
	UpdatedAt:       whereHelpertime_Time{field: "`map_solar_systems`.`updated_at`"},
}

// MapSolarSystemRels is where relationship names are stored.
var MapSolarSystemRels = struct {
}{}

// mapSolarSystemR is where relationships are stored.
type mapSolarSystemR struct {
}

// NewStruct creates a new relationship struct
func (*mapSolarSystemR) NewStruct() *mapSolarSystemR {
	return &mapSolarSystemR{}
}

// mapSolarSystemL is where Load methods for each relationship are stored.
type mapSolarSystemL struct{}

var (
	mapSolarSystemAllColumns            = []string{"id", "name", "constellation_id", "security_class", "security_status", "star_id", "created_at", "updated_at"}
	mapSolarSystemColumnsWithoutDefault = []string{"id", "name", "constellation_id", "security_class", "security_status", "star_id", "created_at", "updated_at"}
	mapSolarSystemColumnsWithDefault    = []string{}
	mapSolarSystemPrimaryKeyColumns     = []string{"id"}
)

type (
	// MapSolarSystemSlice is an alias for a slice of pointers to MapSolarSystem.
	// This should almost always be used instead of []MapSolarSystem.
	MapSolarSystemSlice []*MapSolarSystem
	// MapSolarSystemHook is the signature for custom MapSolarSystem hook methods
	MapSolarSystemHook func(context.Context, boil.ContextExecutor, *MapSolarSystem) error

	mapSolarSystemQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	mapSolarSystemType                 = reflect.TypeOf(&MapSolarSystem{})
	mapSolarSystemMapping              = queries.MakeStructMapping(mapSolarSystemType)
	mapSolarSystemPrimaryKeyMapping, _ = queries.BindMapping(mapSolarSystemType, mapSolarSystemMapping, mapSolarSystemPrimaryKeyColumns)
	mapSolarSystemInsertCacheMut       sync.RWMutex
	mapSolarSystemInsertCache          = make(map[string]insertCache)
	mapSolarSystemUpdateCacheMut       sync.RWMutex
	mapSolarSystemUpdateCache          = make(map[string]updateCache)
	mapSolarSystemUpsertCacheMut       sync.RWMutex
	mapSolarSystemUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var mapSolarSystemBeforeInsertHooks []MapSolarSystemHook
var mapSolarSystemBeforeUpdateHooks []MapSolarSystemHook
var mapSolarSystemBeforeDeleteHooks []MapSolarSystemHook
var mapSolarSystemBeforeUpsertHooks []MapSolarSystemHook

var mapSolarSystemAfterInsertHooks []MapSolarSystemHook
var mapSolarSystemAfterSelectHooks []MapSolarSystemHook
var mapSolarSystemAfterUpdateHooks []MapSolarSystemHook
var mapSolarSystemAfterDeleteHooks []MapSolarSystemHook
var mapSolarSystemAfterUpsertHooks []MapSolarSystemHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MapSolarSystem) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mapSolarSystemBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MapSolarSystem) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mapSolarSystemBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MapSolarSystem) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mapSolarSystemBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MapSolarSystem) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mapSolarSystemBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MapSolarSystem) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mapSolarSystemAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MapSolarSystem) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mapSolarSystemAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MapSolarSystem) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mapSolarSystemAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MapSolarSystem) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mapSolarSystemAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MapSolarSystem) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range mapSolarSystemAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMapSolarSystemHook registers your hook function for all future operations.
func AddMapSolarSystemHook(hookPoint boil.HookPoint, mapSolarSystemHook MapSolarSystemHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		mapSolarSystemBeforeInsertHooks = append(mapSolarSystemBeforeInsertHooks, mapSolarSystemHook)
	case boil.BeforeUpdateHook:
		mapSolarSystemBeforeUpdateHooks = append(mapSolarSystemBeforeUpdateHooks, mapSolarSystemHook)
	case boil.BeforeDeleteHook:
		mapSolarSystemBeforeDeleteHooks = append(mapSolarSystemBeforeDeleteHooks, mapSolarSystemHook)
	case boil.BeforeUpsertHook:
		mapSolarSystemBeforeUpsertHooks = append(mapSolarSystemBeforeUpsertHooks, mapSolarSystemHook)
	case boil.AfterInsertHook:
		mapSolarSystemAfterInsertHooks = append(mapSolarSystemAfterInsertHooks, mapSolarSystemHook)
	case boil.AfterSelectHook:
		mapSolarSystemAfterSelectHooks = append(mapSolarSystemAfterSelectHooks, mapSolarSystemHook)
	case boil.AfterUpdateHook:
		mapSolarSystemAfterUpdateHooks = append(mapSolarSystemAfterUpdateHooks, mapSolarSystemHook)
	case boil.AfterDeleteHook:
		mapSolarSystemAfterDeleteHooks = append(mapSolarSystemAfterDeleteHooks, mapSolarSystemHook)
	case boil.AfterUpsertHook:
		mapSolarSystemAfterUpsertHooks = append(mapSolarSystemAfterUpsertHooks, mapSolarSystemHook)
	}
}

// OneG returns a single mapSolarSystem record from the query using the global executor.
func (q mapSolarSystemQuery) OneG(ctx context.Context) (*MapSolarSystem, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single mapSolarSystem record from the query.
func (q mapSolarSystemQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MapSolarSystem, error) {
	o := &MapSolarSystem{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: failed to execute a one query for map_solar_systems")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all MapSolarSystem records from the query using the global executor.
func (q mapSolarSystemQuery) AllG(ctx context.Context) (MapSolarSystemSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all MapSolarSystem records from the query.
func (q mapSolarSystemQuery) All(ctx context.Context, exec boil.ContextExecutor) (MapSolarSystemSlice, error) {
	var o []*MapSolarSystem

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "boiler: failed to assign all query results to MapSolarSystem slice")
	}

	if len(mapSolarSystemAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all MapSolarSystem records in the query, and panics on error.
func (q mapSolarSystemQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all MapSolarSystem records in the query.
func (q mapSolarSystemQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to count map_solar_systems rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q mapSolarSystemQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q mapSolarSystemQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "boiler: failed to check if map_solar_systems exists")
	}

	return count > 0, nil
}

// MapSolarSystems retrieves all the records using an executor.
func MapSolarSystems(mods ...qm.QueryMod) mapSolarSystemQuery {
	mods = append(mods, qm.From("`map_solar_systems`"))
	return mapSolarSystemQuery{NewQuery(mods...)}
}

// FindMapSolarSystemG retrieves a single record by ID.
func FindMapSolarSystemG(ctx context.Context, iD uint, selectCols ...string) (*MapSolarSystem, error) {
	return FindMapSolarSystem(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindMapSolarSystem retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMapSolarSystem(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*MapSolarSystem, error) {
	mapSolarSystemObj := &MapSolarSystem{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `map_solar_systems` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, mapSolarSystemObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: unable to select from map_solar_systems")
	}

	if err = mapSolarSystemObj.doAfterSelectHooks(ctx, exec); err != nil {
		return mapSolarSystemObj, err
	}

	return mapSolarSystemObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *MapSolarSystem) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MapSolarSystem) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no map_solar_systems provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(mapSolarSystemColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	mapSolarSystemInsertCacheMut.RLock()
	cache, cached := mapSolarSystemInsertCache[key]
	mapSolarSystemInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			mapSolarSystemAllColumns,
			mapSolarSystemColumnsWithDefault,
			mapSolarSystemColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(mapSolarSystemType, mapSolarSystemMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(mapSolarSystemType, mapSolarSystemMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `map_solar_systems` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `map_solar_systems` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `map_solar_systems` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, mapSolarSystemPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "boiler: unable to insert into map_solar_systems")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to populate default values for map_solar_systems")
	}

CacheNoHooks:
	if !cached {
		mapSolarSystemInsertCacheMut.Lock()
		mapSolarSystemInsertCache[key] = cache
		mapSolarSystemInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single MapSolarSystem record using the global executor.
// See Update for more documentation.
func (o *MapSolarSystem) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the MapSolarSystem.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MapSolarSystem) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	mapSolarSystemUpdateCacheMut.RLock()
	cache, cached := mapSolarSystemUpdateCache[key]
	mapSolarSystemUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			mapSolarSystemAllColumns,
			mapSolarSystemPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("boiler: unable to update map_solar_systems, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `map_solar_systems` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, mapSolarSystemPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(mapSolarSystemType, mapSolarSystemMapping, append(wl, mapSolarSystemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update map_solar_systems row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by update for map_solar_systems")
	}

	if !cached {
		mapSolarSystemUpdateCacheMut.Lock()
		mapSolarSystemUpdateCache[key] = cache
		mapSolarSystemUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q mapSolarSystemQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q mapSolarSystemQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all for map_solar_systems")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected for map_solar_systems")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o MapSolarSystemSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MapSolarSystemSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("boiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), mapSolarSystemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `map_solar_systems` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, mapSolarSystemPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all in mapSolarSystem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected all in update all mapSolarSystem")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *MapSolarSystem) UpsertG(ctx context.Context, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateColumns, insertColumns)
}

var mySQLMapSolarSystemUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MapSolarSystem) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no map_solar_systems provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(mapSolarSystemColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLMapSolarSystemUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	mapSolarSystemUpsertCacheMut.RLock()
	cache, cached := mapSolarSystemUpsertCache[key]
	mapSolarSystemUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			mapSolarSystemAllColumns,
			mapSolarSystemColumnsWithDefault,
			mapSolarSystemColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			mapSolarSystemAllColumns,
			mapSolarSystemPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("boiler: unable to upsert map_solar_systems, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`map_solar_systems`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `map_solar_systems` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(mapSolarSystemType, mapSolarSystemMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(mapSolarSystemType, mapSolarSystemMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "boiler: unable to upsert for map_solar_systems")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(mapSolarSystemType, mapSolarSystemMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to retrieve unique values for map_solar_systems")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to populate default values for map_solar_systems")
	}

CacheNoHooks:
	if !cached {
		mapSolarSystemUpsertCacheMut.Lock()
		mapSolarSystemUpsertCache[key] = cache
		mapSolarSystemUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single MapSolarSystem record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *MapSolarSystem) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single MapSolarSystem record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MapSolarSystem) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("boiler: no MapSolarSystem provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), mapSolarSystemPrimaryKeyMapping)
	sql := "DELETE FROM `map_solar_systems` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete from map_solar_systems")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by delete for map_solar_systems")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q mapSolarSystemQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q mapSolarSystemQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("boiler: no mapSolarSystemQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from map_solar_systems")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for map_solar_systems")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o MapSolarSystemSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MapSolarSystemSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(mapSolarSystemBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), mapSolarSystemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `map_solar_systems` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, mapSolarSystemPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from mapSolarSystem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for map_solar_systems")
	}

	if len(mapSolarSystemAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *MapSolarSystem) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("boiler: no MapSolarSystem provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MapSolarSystem) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMapSolarSystem(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MapSolarSystemSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("boiler: empty MapSolarSystemSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MapSolarSystemSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MapSolarSystemSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), mapSolarSystemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `map_solar_systems`.* FROM `map_solar_systems` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, mapSolarSystemPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to reload all in MapSolarSystemSlice")
	}

	*o = slice

	return nil
}

// MapSolarSystemExistsG checks if the MapSolarSystem row exists.
func MapSolarSystemExistsG(ctx context.Context, iD uint) (bool, error) {
	return MapSolarSystemExists(ctx, boil.GetContextDB(), iD)
}

// MapSolarSystemExists checks if the MapSolarSystem row exists.
func MapSolarSystemExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `map_solar_systems` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "boiler: unable to check if map_solar_systems exists")
	}

	return exists, nil
}
